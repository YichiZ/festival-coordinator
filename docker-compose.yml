# Postgres is now managed by the Supabase CLI.
# Run `supabase start` to spin up a local Postgres + API stack, then set:
#   SUPABASE_URL=http://localhost:54321
#   SUPABASE_API_KEY=<anon key printed by supabase start>
#
# Add any other local-only services (e.g. Redis, mock Twilio) here if needed.
